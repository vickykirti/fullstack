Instructions:

git clone "" ->
cd "choose the project name"

cd server ->
npm i ->
npm start

cd client ->
npm i ->
npm start

This project uses the MongoDB Compass as the database management system.
